## dev-prefix.conf
# Map /dev/ to the Apache document root so the dev instance is reachable under /dev/
Alias /dev/ /var/www/html/

<Directory /var/www/html/>
    Options Indexes FollowSymLinks
    AllowOverride All
    Require all granted
</Directory>
